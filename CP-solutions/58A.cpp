#include<bits/stdc++.h>
using namespace std;
int main()
{
  string s;
  cin>>s;
  vector<string> v;
  v.push_back(s);
  for (int i = 0; i < v.size(); i++)
  {
         if ((v[i]==v[i+1]&&v[i]!='l')||v[i]!=)
         {
                /* code */
         }
  }
  return 0;
}